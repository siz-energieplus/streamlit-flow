# custom port for the streamlit-flow component as default port 3000 is already
# taken by the streamlit server itself
PORT=${STREAMLIT_FLOW_PORT}

# Don't automatically open the web browser on `npm run start`.
BROWSER=none
