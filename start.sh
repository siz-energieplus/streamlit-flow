#!/bin/bash
# when you checkout this file on windows, make sure to change the line endings from CRLF
# to just LF as the script will otherwise error within the docker environment
cd streamlit_flow/frontend
npm run start